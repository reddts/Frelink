# Frelink 管理端模板更新计划（Vue-Vben-Admin 版）

## 当前推进状态

  - `2026-04-03`：
  - 已为新版管理端登录页补低高度视口单屏规则：
    - 登录页已新增专用 `login-screen` 布局类，不再只依赖通用留白
    - 视口高度低于 `860px` 时会自动隐藏左侧封面，优先保留登录卡
    - 视口高度低于 `760px` 时会继续压缩登录卡内边距
  - 本轮已完成真实闭环：
    - 本地 `corepack pnpm --dir admin-vben typecheck` 通过
    - 本地 `corepack pnpm --dir admin-vben build` 通过
    - 构建产物已写入 `public/admin-vben/`
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
  - `2026-04-03`：
  - 已为新版管理端 Dashboard 增加待审文章统计：
    - 仪表盘统计卡已新增 `待审文章`
    - 当前 Dashboard 现可同时查看待审文章、待审问题、待审回答
    - 本轮只扩展后端统计 payload，不额外增加前端模板复杂度
  - 本轮已完成真实闭环：
    - 本地 `corepack pnpm --dir admin-vben typecheck` 通过
    - 本地 `corepack pnpm --dir admin-vben build` 通过
    - 构建产物已写入 `public/admin-vben/`
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
  - `2026-04-03`：
  - 已继续压缩新管理端登录页高度并恢复 Linux 本地构建链：
    - 登录页已改为单屏优先布局，左右区块与上下留白已明显收紧
    - 左侧封面已限制为 `lg` 及以上显示，避免小屏和低高度窗口被封面内容撑高
    - WSL 本地已补 `nvm + Node LTS + corepack pnpm`，不再依赖 Windows 侧损坏的 `pnpm` 入口
    - `admin-vben` 已补齐 `tailwindcss` 与 `@tailwindcss/vite` 依赖声明
  - 本轮已完成真实闭环：
    - 本地 `corepack pnpm --dir admin-vben typecheck` 通过
    - 本地 `corepack pnpm --dir admin-vben build` 通过
    - 构建产物已写入 `public/admin-vben/`
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
    - 已完成线上 smoke check：`/`、`/questions/`、`/articles/`
  - `2026-04-03`：
  - 已继续把系统管理高频页迁到统一后台基线：
    - 用户、管理组、菜单、权限、配置 五个系统管理页已统一切到 `Button / Input / Textarea` 基线语义
    - 各页已补概览卡片，开始统一后台页的筛选、列表、编辑器节奏
    - `Input / Textarea` 已补组件级 `v-model` 能力，避免基线组件停留在“只包一层样式”
  - `2026-04-03`：
  - 已完成登录页正式化重做：
    - 登录卡已明显加宽，输入区域不再过窄
    - 登录页已去除迁移规划式文案，改为正式后台视觉语言
    - 左侧封面已改为正式化的运营 / 监控视觉表达
  - `2026-04-03`：
  - 已继续把登录外高频通用层迁到官方基线：
    - 壳层按钮已开始切到 `Button`
    - 统一内容编辑器已切到 `Button / Input / Textarea`
    - 文章页和问题页的搜索、状态切换、批量操作已切到官方基线组件
  - `2026-04-03`：
  - 已完成“官方依赖基线接入”第一步：
    - 子工程已接入 `Tailwind 4` 与 Shadcn 常用基础依赖
    - 已补 `Button / Card / Input` 三个最小基线组件
    - 登录页和仪表盘已先迁到这套基线
    - 下一轮可继续把卡片、表单、表格、导航逐步收口到同一体系
  - `2026-04-03`：
  - 已继续收口新管理端导航规范：
    - 侧栏菜单已支持按分组折叠，并记忆分组展开状态
    - 已明确后续优先使用 `admin-vben` 官方组件和官方样式语义，不再持续扩写自定义基础组件与自定义视觉规范
  - `2026-04-03`：
  - 已补新管理端全局 loading 动画：
    - 首屏 bootstrap 已有 loading 遮罩和动画
    - 路由切换已显示页面载入反馈
    - 登录 / 退出已显示专用 loading 文案
  - `2026-04-03`：
  - 已完成新管理端仪表盘的一轮图形化升级：
    - 仪表盘不再只有数字卡，已补占比指标和 7 日趋势图
    - 已可直接查看访问覆盖率、用户活跃度、机器人参与度、审核处理率
    - 已可直接查看内容新增、用户活跃、Agent 参与的趋势变化
  - `2026-04-03`：
  - 已补新管理端版本标识：
    - 顶栏已显示框架基线 `Vben Admin 5.x`
    - 顶栏已显示本地工程版本 `v0.1.0`
    - 本地版本已从 `admin-vben/package.json` 收口读取，便于后续持续维护
  - `2026-04-03`：
  - 已完成新管理端壳层导航的一轮优化：
    - 侧栏菜单已改为默认折叠，减少内容区被长菜单持续挤压
    - 顶部已补菜单展开 / 折叠开关
    - 侧栏状态已做本地持久化，刷新后沿用用户上次选择
  - `2026-04-03`：
  - `M2` 已继续推进到“回答 / 审核 复用统一编辑器模型”：
    - 回答页已切换到统一内容编辑器，不再单独维护回答正文表单模板
    - 审核详情已补只读候选内容模型，可按问题 / 文章 / 回答类型直接查看结构化标题、正文和 SEO
    - 统一编辑器组件已扩展为支持只读 / 隐藏字段 / 禁用提交动作的可配置模型
  - 本轮已完成真实闭环：
    - 本地 `typecheck` 与 `build` 通过
    - 构建产物已写入 `public/admin-vben/`
  - `2026-04-03`：
  - 已修复线上实测仍存在的新管理端登录 500：
    - 已确认真实根因不是前端缓存，而是生产环境 `kn_admin_auth` 表缺失 `menu` 字段
    - 登录成功后的 bootstrap 菜单树查询已移除对 `menu` 字段的硬依赖
    - 权限节点保存链路已补表结构探测，只在生产表真实存在该字段时才写入
  - 本轮已完成真实闭环：
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
    - 已基于线上访问日志确认此前 `POST /adminapi.php/Admin/login` 的 500 触发点
    - 新管理端登录链路已与当前生产库表结构对齐
  - `2026-04-03`：
  - `M2` 已继续推进到“统一表单模型”起步：
    - 已新增内容编辑表单复用组件
    - 文章 / 问题 页已从“只改 SEO”推进到“直接修改标题 + 主体内容 + SEO”
    - 内容编辑器已开始复用统一表单模型，不再分别维护两套近似表单
  - `2026-04-03`：
  - 已修复新版管理端登录阶段的潜在 500：
    - `adminapi` 菜单树已对历史脏菜单、外链、`javascript:` 与异常规则名做容错
    - 登录成功后的 bootstrap 不再因为 `legacy_url / path` 构造失败而中断
  - `M2` 已继续推进到“统一详情模型”起步：
    - 已新增内容详情面板复用组件
    - 文章 / 问题 / 回答 / 审核 四个高频内容页已切到统一详情面板
    - 审核详情的摘要字段与结构化预览字段已开始走统一展示模型
  - 本轮已完成真实闭环：
    - 本地 `typecheck` 与 `build` 通过
    - 构建产物已写入 `public/admin-vben/`
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
    - 已确认 `/admin-vben/` 线上引用本轮最新资源
    - 已确认 `Admin/login` 未登录链路在线返回稳定 JSON，空参未再出现 500
  - `2026-04-02`：
  - `M0` 已完成
  - `M1` 已完成首轮基线接入
  - `M2` 已完成系统管理子阶段收口：
    - 权限节点页已迁入新管理端
    - 管理组页已从只读推进到可编辑
    - 菜单页已从只读推进到可编辑
    - 配置页已保持可编辑并继续可用
    - 用户页已从“列表 + 单用户编辑”推进到“批量动作 + 积分链路”
  - `M2` 已完成内容模块首轮接入：
    - 审核页已迁入新管理端并具备通过 / 拒绝 / 封禁动作
    - 文章页已具备列表、SEO、删除、恢复
    - 问题页已具备列表、SEO、删除、恢复
    - 回答页已具备列表、正文编辑、删除
  - `M2` 已完成内容模块第二轮增强：
    - 文章页已补齐批量删除 / 批量恢复 / 批量彻底删除 / 前台预览 / 发布页跳转
    - 问题页已补齐批量删除 / 批量恢复 / 批量彻底删除 / 前台预览 / 发布页跳转
    - 回答页已补齐批量删除 / 批量彻底删除 / 前台预览
    - 审核页已补齐批量通过 / 批量拒绝 / 批量封禁用户 / 批量封禁 IP / 前台预览
  - `M2` 已完成内容模块第三轮增强：
    - 文章 / 问题 / 回答 详情面板已补齐结构化字段预览
    - 审核详情已补齐按类型输出的结构化预览，不再只有原始 JSON
    - 审核详情已补齐 Agent 只读字段结构化展示
  - `M2` 已完成内容模块第四轮增强：
    - 文章页已补齐推荐 / 置顶 / 赞同等发布态标签展示
    - 问题页已补齐悬赏 / 匿名 / 推荐 / 置顶等发布态标签展示
    - 回答页已补齐关键词筛选、感谢数与最佳回答 / 匿名标签
    - 审核详情已继续补齐推荐 / 置顶 / 匿名 / 最佳回答 / 关联话题等发布态预览字段
  - 已完成真实闭环：
    - Windows 侧重新安装 `admin-vben` 依赖
    - 本地 `typecheck` 与 `build` 通过
    - 构建产物已写入 `public/admin-vben/`
    - 已执行 `bash scripts/deploy.sh sync`
    - 已执行 `bash scripts/deploy.sh verify`
    - 已确认 `/admin-vben/` 线上引用本轮最新资源
    - 已确认 `SystemAuth` 线上接口在未登录态返回标准 `AUTH_REQUIRED`
    - 已确认 `ContentApproval` 与 `ContentArticle` 线上接口在未登录态返回标准 `AUTH_REQUIRED`
  - 已落地：
    - `admin-vben` 子工程
    - 后台登录 / 当前管理员 / 菜单 / 仪表盘 JSON 接口
    - `/admin-vben/` 静态入口
    - 新壳层下的动态菜单与旧页占位路由
    - 权限节点 / 管理组 / 菜单 / 配置 / 用户 五个系统管理页
    - 审核 / 文章 / 问题 / 回答 四个高频内容页第四轮版本
  - 下一轮主目标：
    - 继续把高频内容后台操作从“文章 / 问题统一表单模型”推进到“回答 / 审核 等更多页面模型复用”
    - 继续补 `回答 / 审核` 的更完整编辑链路，并把详情 / 编辑器模型继续复用
    - 视优先级继续补用户详情深层操作，或转入下一组后台业务模块
  - 补充校正：
    - 后台新接口最终必须独立收口到 `adminapi` 体系，不继续并入现有前台 `api` 语义
    - 新管理端必须按独立 Vue 工程长期维护，本地安装依赖、本地测试、编译产物投放 `public` 后直接运行
    - 后续迁移重点是后台业务链路重构，不是只替换界面外壳

## 1. 文档目的

本文档用于指导将当前 `Frelink` 管理端从现有 PHP 模板后台，逐步迁移到基于 `Vue-Vben-Admin` 的新管理端框架，并确保迁移过程严格遵守项目现有推进规范。

本文档重点覆盖：

- 当前后台现状盘点
- 模板选型与前置决策
- 迁移目标与非目标
- 分阶段迁移计划
- 前后端改造约束
- 菜单、权限、页面适配规则
- 测试、发布、回滚与验收流程
- 每轮推进闭环要求

本文档不包含：

- 前台页面改版
- 业务表结构的大规模重构
- 一次性替换所有旧后台页面的硬切方案
- 脱离现有 PHP 后端的全栈重写

## 2. 前置决策

### 2.1 模板选型结论

- 用户指定参考仓库：`https://github.com/vbenjs/vue-vben-admin-doc`
- 该仓库是文档仓库，不是实际业务模板仓库。
- 该仓库已于 `2024-09-30` 被归档，只读，不适合作为实施基线。
- 实际迁移基线应使用活跃主仓库：`https://github.com/vbenjs/vue-vben-admin`
- 当前公开文档为 `Vben Admin 5.x`：`https://doc.vben.pro/`

### 2.2 本项目采用策略

- 文档参考源：`vue-vben-admin-doc` 的历史内容 + `doc.vben.pro`
- 工程实施基线：`vue-vben-admin` 主仓库当前稳定版本
- 迁移目标形态：新增独立前端管理端应用，不在旧 PHP 模板里做“套皮式改造”
- 迁移方式：新旧后台并行，逐模块切换，不允许一步替换
- 服务端接口策略：后台新接口独立命名为 `adminapi`，不继续接入现有前台 `api` 体系
- 构建策略：新管理端作为独立 Vue 子工程维护，编译产物直接发布到 `public` 中运行
- 入口策略：当前阶段后台独立接口先通过 `public/adminapi.php` 提供；若后续需要无扩展名的 `/adminapi/`，需单独补服务器 rewrite
- 收口策略：当 `adminapi + Vue` 新管理端完成完整测试与线上验证后，最终要彻底删除 `app/backend`，并确保其中原有功能依赖已全部迁出

### 2.3 采用 Vue-Vben-Admin 的理由

- 支持 `Vue 3`、`Vite`、`TypeScript`
- 具备动态菜单、权限控制、主题、国际化等中后台基础能力
- 适合做独立前端后台，而不是继续加深服务端模板耦合
- 工程化能力完整，适合后续把后台改造成可持续演进的前后端分层结构

### 2.4 新管理端实现规范补充

- 后续新页面、新壳层能力和新交互，默认优先采用 `admin-vben` 官方组件、官方布局能力和官方样式语义，不再把自定义基础组件作为常态方案。
- 若当前子工程缺失对应官方组件依赖，应优先评估并补齐官方依赖，再进入页面实现；不能因为“当前没接”就继续长期堆叠自定义基础 UI。
- 只有在官方组件无法覆盖业务需求，或现阶段迁移成本明显过高时，才允许保留极小范围的兼容性自定义实现；此类实现必须在文档中注明原因和后续收口目标。
- 新增样式应优先复用框架已有变量、间距体系、状态色和交互语义；避免继续扩散独立命名、独立视觉语言和一次性样式片段。
- 导航、表单、表格、详情、弹层、加载态等后台通用交互，默认作为框架能力统一收口，不允许每个页面分别拼装一套近似实现。

## 3. 当前后台现状

### 3.1 后台入口与基础结构

- 后台入口：`public/admin.php`
- 后台控制器基类：`app/common/controller/Backend.php`
- 后台首页控制器：`app/backend/Index.php`
- 当前后台模式：
  - PHP 模板直出
  - `TableBuilder` / `FormBuilder`
  - 服务端模板 + 局部 AJAX + iframe 弹窗 + PJAX 混合

### 3.2 当前后台模板关键文件

- 公共壳层：`app/backend/view/block.php`
- 表单布局：`app/backend/view/global/form/layout.php`
- 列表布局：`app/backend/view/global/table/layout.php`
- 登录页：`app/backend/view/index/login.php`

### 3.3 当前后台模块清单

#### 首页与基础页

- `app/backend/Index.php`
- `app/backend/Explore.php`

#### 系统管理

- `app/backend/admin/Auth.php`
- `app/backend/admin/Config.php`
- `app/backend/admin/Dict.php`
- `app/backend/admin/DictType.php`
- `app/backend/admin/Group.php`
- `app/backend/admin/Menu.php`
- `app/backend/admin/Theme.php`

#### 内容管理

- `app/backend/content/Announce.php`
- `app/backend/content/Answer.php`
- `app/backend/content/Approval.php`
- `app/backend/content/Article.php`
- `app/backend/content/Category.php`
- `app/backend/content/Column.php`
- `app/backend/content/Feature.php`
- `app/backend/content/Help.php`
- `app/backend/content/Page.php`
- `app/backend/content/Question.php`
- `app/backend/content/Report.php`
- `app/backend/content/Topic.php`

#### 扩展能力

- `app/backend/extend/Database.php`
- `app/backend/extend/Links.php`
- `app/backend/extend/Log.php`
- `app/backend/extend/RouteRule.php`
- `app/backend/extend/Task.php`
- `app/backend/extend/Token.php`

#### 会员管理

- `app/backend/member/Action.php`
- `app/backend/member/Forbidden.php`
- `app/backend/member/IntegralGroup.php`
- `app/backend/member/IntegralRule.php`
- `app/backend/member/NotifySetting.php`
- `app/backend/member/Permission.php`
- `app/backend/member/ReputationGroup.php`
- `app/backend/member/Users.php`
- `app/backend/member/Verify.php`

#### 插件与微信

- `app/backend/plugin/Plugins.php`
- `app/backend/plugin/Upgrade.php`
- `app/backend/wechat/Account.php`
- `app/backend/wechat/TemplateMessage.php`
- `app/backend/wechat/WeChatFactor.php`

### 3.4 当前问题总结

- 后台页面渲染方式老旧，页面结构、交互逻辑、模板样式高度耦合。
- 列表页和表单页依赖服务端 Builder，导致前端复用、状态管理和交互增强困难。
- 菜单、权限、路由仍以服务端模板为核心，不适合现代 SPA 后台。
- 弹窗、iframe、PJAX 混用，维护成本高，回归测试困难。
- 新功能继续写在旧后台，会持续扩大技术债。
- 当前缺少针对后台页面的稳定自动化测试链路。

## 4. 总目标与边界

### 4.1 总目标

构建一个新的独立管理端应用，基于 `Vue-Vben-Admin` 作为前端壳层，完成对现有后台核心功能与页面的逐步迁移，最终实现：

- 独立前端管理端工程
- 基于独立 `adminapi` 的后台页面交互
- 统一菜单、权限、路由、表格、表单、详情、上传和通知体系
- 支持旧后台与新后台双轨并行
- 支持按模块切换、按阶段回退
- 后续新功能优先落在新管理端
- 本地可安装、可测试、可编译，编译后可直接投放 `public` 运行
- 以原有模板后台功能为参照完成业务重构，而不是只做视觉替换

### 4.2 非目标

- 首阶段不删除旧后台控制器与模板
- 首阶段不重构业务表结构
- 首阶段不强行统一所有插件后台
- 首阶段不要求所有页面一次性交付
- 不允许把旧后台整个塞进新模板 iframe 里当成“迁移完成”
- 但在迁移完成阶段，`app/backend` 不作为长期保留目录，最终目标是完整删除而不是永久并存

### 4.3 迁移原则

- 先接口化，再页面化
- 先高频模块，再低频模块
- 先双轨并行，再收口替换
- 新功能默认优先进入新管理端
- 每迁移一个模块，必须补最小验证与记录
- 页面“看起来像新模板”不算完成，必须业务链路可用
- 只改后台框架不算完成，必须同步完成对应业务链路重构
- 新后台接口、前端页面、编译发布方式必须同时成立，才算有效迁移
- 旧后台目录中的控制器、模板、Builder 和隐式耦合依赖都要逐步拆离，最终删除 `app/backend`

## 5. 目标架构

### 5.1 推荐目录结构

```text
/
├── app/
├── public/
├── docs/
├── admin-vben/
│   ├── apps/
│   ├── packages/
│   ├── internal/
│   ├── .env.development
│   ├── .env.production
│   ├── package.json
│   └── pnpm-workspace.yaml
└── ...
```

### 5.2 Frelink 建议落地结构

如果不直接保留上游完整 monorepo，可采用“业务子工程”方式：

```text
/
├── app/
├── adminapi/
│   ├── controller/
│   ├── middleware/
│   └── ...
├── public/
│   ├── admin-vben/
│   └── ...
├── docs/
├── admin-vben/
│   ├── src/
│   │   ├── api/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── views/
│   │   │   ├── dashboard/
│   │   │   ├── system/
│   │   │   ├── content/
│   │   │   ├── member/
│   │   │   ├── extend/
│   │   │   ├── plugin/
│   │   │   └── wechat/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── constants/
│   │   └── utils/
│   ├── mock/
│   ├── tests/
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
└── ...
```

### 5.3 架构分层

#### 前端层

- 登录页
- 布局壳层
- 动态菜单与路由
- 列表页、表单页、详情页通用组件
- 统一请求层
- 统一权限守卫

#### 服务端层

- 登录认证 `AdminApi`
- 当前管理员信息 `AdminApi`
- 菜单树 `AdminApi`
- 列表 / 详情 / 保存 / 删除 `AdminApi`
- 上传 `AdminApi`
- 统计 `AdminApi`
- 审批 / 日志 / 任务类 `AdminApi`

#### 兼容层

- 旧后台控制器继续存在
- 迁移期允许旧模板页和新 SPA 并存
- 通过开关控制菜单入口走旧页还是新页
- 兼容层只在迁移期间存在，不作为长期架构；最终收口时需移除 `app/backend`

## 6. 技术选型与实施约束

### 6.1 前端技术约束

- 使用 `Vue 3 + TypeScript + Vite`
- 使用 `pnpm`
- 必须先在本地完成依赖安装、启动和构建验证，再进入服务器同步
- 使用统一 `request` 封装，不允许页面内散落裸请求
- 所有页面必须拆为可复用业务组件，不允许大页面堆逻辑
- 菜单、路由、权限必须统一接入，不允许页面各自实现权限判断
- 列表、表单、详情页要抽通用页面模型，避免每页重新造轮子
- 编译产物必须可直接发布到 `public/admin-vben/` 运行
- 新管理端目录必须独立维护，不与旧 PHP 模板文件混写

### 6.2 服务端技术约束

- 旧控制器保留，但新管理端不得依赖 HTML 输出
- 后台新接口命名空间统一为 `adminapi`
- 不继续复用现有面向前台/开放调用的 `api` 作为后台迁移主接口
- 新接口必须返回稳定 JSON 结构
- 分页格式必须统一
- 错误码必须统一
- 登录失效、权限不足、校验失败、业务失败要可机器识别
- 菜单和按钮权限必须能被前端消费
- 命名、目录、鉴权和返回结构都要按后台独立体系整理，避免与前台开放接口长期混用

### 6.3 接口统一返回建议

成功：

```json
{
  "code": 1,
  "msg": "ok",
  "data": {},
  "request_id": "uuid"
}
```

失败：

```json
{
  "code": 0,
  "msg": "error",
  "error_code": "PERMISSION_DENIED",
  "data": null,
  "request_id": "uuid"
}
```

分页：

```json
{
  "code": 1,
  "msg": "ok",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "per_page": 20
  },
  "request_id": "uuid"
}
```

## 7. 菜单、权限与页面适配规则

### 7.1 菜单迁移规则

- 以现有后台菜单权限为源数据，不重造一套独立权限语义
- 先做菜单读取适配，再做页面显示适配
- 菜单项必须标记：
  - 页面名称
  - 路由路径
  - 对应旧控制器
  - 权限标识
  - 当前迁移状态

### 7.2 权限迁移规则

- 页面权限继承现有后台权限模型
- 按钮级权限同步暴露给前端
- 前端只负责展示与拦截，最终鉴权必须仍由后端完成
- 权限缺失时，页面不得只隐藏按钮，接口也必须拒绝

### 7.3 页面适配规则

- 原列表页迁移为统一 `ListPage`
- 原新增/编辑页迁移为统一 `FormPage`
- 原详情页或审核页迁移为统一 `DetailPage`
- 原弹窗操作尽量收敛为抽屉或模态框，不继续保留 iframe 弹层模式
- 原 PJAX 行为统一收口到前端路由和状态刷新机制

## 8. 模块迁移优先级

### 8.1 P0：首批必须迁移

- 登录页
- 首页 / 仪表盘
- 系统管理：
  - 管理员
  - 管理组
  - 菜单
  - 配置
- 内容管理：
  - 文章
  - 问题
  - 回答
  - 审核
- 会员管理：
  - 用户
  - 认证
  - 封禁

### 8.2 P1：第二批迁移

- 话题
- 分类
- 栏目
- 公告
- 专题 / 功能位
- 积分组 / 规则
- 声望组 / 权限
- 操作记录 / 日志
- Token 管理

### 8.3 P2：第三批迁移

- 数据库工具
- 路由规则
- 任务调度
- 友情链接
- 插件管理
- 升级
- 微信相关模块

### 8.4 可延后项

- 低频插件后台
- 仅运维使用且改动不频繁的页面
- 重度依赖旧 Builder 且当前业务价值低的辅助页

## 9. 分阶段实施计划

## M0 立项与基线

### 目标

- 明确迁移范围、模块优先级、风险和回滚策略
- 确认 `Vue-Vben-Admin` 版本与接入方式
- 完成当前后台页面与菜单的资产清单

### 必做项

- 输出本计划文档
- 盘点现有后台控制器、模板、菜单、权限节点
- 标注高频页、复杂页、低频页
- 明确新管理端部署方式：
  - 同域部署
  - 独立静态资源目录
  - 接口代理方式

### 验收

- 有完整迁移清单
- 有明确阶段划分
- 有确定的技术基线与风险表

## M1 框架接入与基础能力打通

### 目标

- 初始化 `admin-vben`
- 打通登录、基础布局、接口请求、路由、菜单和权限骨架

### 必做项

- 初始化前端工程
- 接入开发环境与生产环境配置
- 对接登录接口
- 对接当前管理员信息接口
- 对接菜单树接口
- 搭建基础布局、标签页、面包屑、401/403/404 页面
- 打通统一错误处理、登录失效跳转、全局 loading

### 验收

- 能成功登录
- 能拿到管理员信息和菜单
- 能打开首页壳层与基础路由
- 能按权限显示菜单

## M2 核心工作流迁移

### 目标

- 优先迁移高频核心模块，形成可真实使用的新后台主链路

### 必做项

- 首页 / 仪表盘迁移
- 文章管理迁移
- 问题管理迁移
- 回答管理迁移
- 审核管理迁移
- 用户管理迁移
- 管理员 / 管理组 / 菜单 / 配置页迁移

### 验收

- 核心高频后台操作可在新管理端完成
- 列表、搜索、筛选、分页、详情、编辑、审核链路可用
- 旧后台仅作为兜底，不再是主操作入口

## M3 公共能力与页面模型收口

### 目标

- 把迁移中重复出现的页面能力沉淀成可复用模型

### 必做项

- 统一列表页模型
- 统一表单页模型
- 统一详情页模型
- 统一上传组件
- 统一富文本、状态标签、批量操作、导出能力
- 统一权限按钮与操作反馈

### 验收

- 新增后台页不需要从零开始搭
- 同类页面结构、交互、反馈一致
- 迁移速度明显提升

## M4 全量模块迁移与旧后台收口

### 目标

- 迁移剩余低频页和扩展页，逐步减少旧模板后台入口
- 完成 `app/backend` 依赖拆离，为最终删除旧后台目录做准备

### 必做项

- 完成 P1 / P2 模块迁移
- 菜单中标记未迁移与已迁移页面
- 对旧页面做入口收口
- 对无法短期迁移页面做明确保留说明
- 梳理并迁出 `app/backend` 对公共 helper、Builder、模板变量、权限判断的残留依赖
- 清理不再需要的旧后台模板、控制器和配套静态耦合

### 验收

- 新后台覆盖绝大多数管理端功能
- 高频工作不再依赖旧后台
- 菜单结构与页面入口基本统一
- `app/backend` 仅允许保留明确登记的临时兼容项

## M5 测试、部署、切换与回滚完善

### 目标

- 完善自动化测试、部署流程、灰度切换与回滚机制
- 在新后台稳定后完成旧后台目录删除和依赖清理

### 必做项

- 增加接口契约测试
- 增加核心页面 E2E
- 增加构建与发布脚本
- 增加新旧后台切换开关
- 增加异常回退路径
- 在 `adminapi + Vue` 完整测试通过后，删除 `app/backend`
- 删除前确认其功能依赖已迁移到 `adminapi`、公共服务层或前端独立工程

### 验收

- 关键模块有自动化回归
- 新后台可以独立构建和发布
- 可按开关回退旧后台
- 删除 `app/backend` 后，新后台主链路仍可独立运行

## 10. 页面迁移执行清单模板

每迁移一个页面或模块，必须按以下清单执行：

1. 确认旧页面对应控制器、模板、菜单、权限节点
2. 梳理该页涉及的列表、详情、保存、删除、审核、导出接口
3. 若接口仍输出 HTML 或字段不稳定，先做接口层改造
4. 在 `admin-vben` 中建立对应路由与页面目录
5. 实现页面 UI 与交互
6. 接入权限控制
7. 完成本地验证
8. 同步服务器验证
9. 更新 `优化计划.md`
10. 更新 `CHANGELOG.md`
11. 完成中文 commit

## 11. 数据与接口改造范围

### 11.1 需要优先接口化的能力

- `adminapi` 登录与退出登录
- `adminapi` 当前管理员资料
- `adminapi` 菜单树
- `adminapi` 通用分页列表
- `adminapi` 通用详情读取
- `adminapi` 通用保存与编辑
- `adminapi` 通用状态切换
- `adminapi` 通用删除 / 批量操作
- `adminapi` 上传
- `adminapi` 首页统计与洞察数据

### 11.2 需要特别关注的旧后台行为

- Builder 自动生成字段
- iframe 弹窗打开编辑页
- PJAX 局部刷新
- 表格顶部批量操作
- 异步导出
- 原有模板中的联动字段与动态显示逻辑

### 11.3 接口改造原则

- 优先保持业务语义不变，只改输出形式
- 一次只收口一类返回结构，不同时大改多个模块
- 对高频页先做适配层，不先追求接口“绝对完美”
- 后台接口应优先从旧控制器/模型中抽业务能力，逐步沉淀为新的 `adminapi`，而不是把旧模板输出简单包一层
- 接口命名、字段、错误码、分页和权限结构要按新后台统一规范重构，避免继续背负旧命名债务

## 12. 测试与验收规范

### 12.1 本地最小验证

- 前端依赖安装成功
- 前端项目可启动
- 前端项目可构建
- 登录链路可用
- 目标页面可打开
- 列表、搜索、分页、保存、删除至少走通一次
- 控制台无阻断级报错

### 12.2 服务端最小验证

- 相关 PHP 文件 `php -l`
- 相关接口可访问
- 缓存清理或必要刷新命令执行完成
- 涉及菜单或权限时完成账号验证

### 12.3 页面验收标准

- 页面结构完整
- 权限控制正确
- 成功、失败、空态、加载态明确
- 表单校验可用
- 关键业务操作可闭环
- 与旧后台结果一致，或差异已明确记录

### 12.4 自动化测试建议

- 单元测试：
  - 公共工具函数
  - 通用表单 / 表格组件
- 集成测试：
  - 登录
  - 菜单生成
  - 权限控制
- E2E：
  - 文章管理
  - 用户管理
  - 审核流程
  - 配置保存

## 13. 发布、切换与回滚策略

### 13.1 发布策略

- 新管理端先使用独立入口部署
- 不直接覆盖旧后台入口
- 采用灰度方式让管理员先试用

### 13.2 切换策略

- 先开放测试账号
- 再开放部分高频菜单
- 最后切换默认后台入口

### 13.3 回滚策略

- 保留旧后台控制器与模板
- 保留原登录入口
- 菜单可按开关回退到旧页
- 若新管理端异常，可立即恢复旧后台为主入口

## 14. 风险清单

### 风险 1：误把归档文档仓库当作实施基线

- 影响：启动后发现目录、依赖、版本全部过时
- 应对：实施基线统一使用 `vue-vben-admin` 主仓库和 `doc.vben.pro`

### 风险 2：接口层未先收口，前端页面无法稳定推进

- 影响：页面迁移反复返工
- 应对：优先把登录、菜单、列表、表单 API 契约稳定下来

### 风险 3：新旧后台并行期维护成本过高

- 影响：同一功能维护两套页面
- 应对：新功能优先只进新管理端，旧后台只做兜底维护

### 风险 4：过早全量迁移导致交付失控

- 影响：周期过长、无法闭环
- 应对：按 P0、P1、P2 分批，先打通高频链路

### 风险 5：权限、菜单、按钮行为迁移不一致

- 影响：新后台能看不能用，或越权
- 应对：以前后端双重鉴权为硬约束，逐页验收

## 15. 严格执行的项目流程规范

### 15.1 每轮推进固定流程

后续每一轮与管理端模板迁移相关的工作，必须严格按照以下顺序执行：

1. 明确本轮唯一主目标
2. 更新或确认 `管理端模板更新计划.md` 中对应阶段
3. 完成代码改动
4. 完成本地最小验证
5. 同步服务器
6. 完成服务器最小验证
7. 更新 `优化计划.md`
8. 更新 `CHANGELOG.md`
9. 提交一次中文 commit

### 15.2 文档联动规则

- `管理端模板更新计划.md`：负责迁移主计划、阶段、范围、规则
- `优化计划.md`：负责纳入项目级推进目标与当前待办
- `CHANGELOG.md`：负责记录本轮实际完成结果与验证结论

### 15.3 完成定义

一个迁移阶段只有满足以下条件，才允许标记为完成：

- 代码已写入仓库
- 本地验证已完成
- 服务器已同步并完成最小验证
- `优化计划.md` 已更新
- `CHANGELOG.md` 已更新
- 已提交中文 commit

## 16. 建议的首轮执行顺序

### 第一轮

- 建立 `admin-vben` 工程
- 跑通登录、路由、菜单、首页壳层
- 同步明确后台接口独立收口到 `adminapi`

### 第二轮

- 打通管理员、管理组、菜单、配置四个系统模块

### 第三轮

- 打通文章、问题、回答、审核四个核心内容模块

### 第四轮

- 打通用户、认证、封禁等会员主链路

### 第五轮

- 收口扩展模块、低频页、测试、切换、回滚

## 17. 当前结论

- 本项目适合采用 `Vue-Vben-Admin` 作为新管理端模板。
- 但实施基线不能直接使用已归档的 `vue-vben-admin-doc` 仓库。
- 正确路径是：参考旧文档思路，基于活跃的 `vue-vben-admin` 5.x 工程接入，并用新旧后台并行方式完成迁移。
- 迁移成败不取决于“模板是否套上”，而取决于登录、菜单、权限、列表、表单、审核、发布、运维等后台主链路是否真正落地。
- 当 `adminapi + Vue` 主链路完成测试后，旧的 `app/backend` 应作为迁移完成后的清理对象被彻底删除，而不是继续长期保留。

## 18. 参考资料

- `Vue-Vben-Admin` 主仓库：<https://github.com/vbenjs/vue-vben-admin>
- `Vue-Vben-Admin` 当前文档：<https://doc.vben.pro/>
- 已归档文档仓库：<https://github.com/vbenjs/vue-vben-admin-doc>
- 项目推进模板：`docs/project-delivery-template.md`
- 既有后台迁移样板：`docs/ant-design-pro-migration-plan.md`
